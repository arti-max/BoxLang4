$ifndef cli_incl
$define cli_incl 1

namespace cli (

    box puts[char* s] -> void (
        while [*s != 0] (
            open putc[*s];
            s:s+1;
        )
    )

    box putc[char c] -> void (
        asm["psh (c)"];
        asm["int $2"];
    )

)

$endif